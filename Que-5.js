console.log(average(2, 1));

// Define the average function
function average(a, b) {
  // Calculate the average by adding a and dividing the sum by 2
  return (a + b) / 2;
}

// Call the average function with arguments 2 and 1
console.log(average(2, 1));
